{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main workbench window (core events only; the window loads the local engine URL).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
