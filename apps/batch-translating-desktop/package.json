{
  "name": "@moonshot-ai/batch-translating-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri desktop shell for Batch Translating: a native window that starts the local kimi.exe engine and opens the workbench web UI.",
  "license": "MIT",
  "scripts": {
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "icons": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/generate-icons.ps1"
  },
  "devDependencies": {}
}
