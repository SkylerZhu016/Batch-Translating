/**
 * Persisted project metadata still carries this version for compatibility
 * with projects created before the native task-book Coordinator replaced the
 * fixed stage-prompt runner. No system or stage prompt is built in the web UI.
 */
export const TRANSLATION_PROMPT_VERSION = 'batch-translation-prompt-v1.0.0';
