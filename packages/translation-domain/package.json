{
  "name": "@batch-translating/translation-domain",
  "version": "0.1.0",
  "private": true,
  "description": "Durable translation project, task, artifact, instruction, and cost ledger",
  "license": "MIT",
  "type": "module",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=24.15.0"
  }
}
